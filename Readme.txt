python snmp inventory

The main goals of this projects is monitoring/inventory devices througth snmp protocol.
This project use pysnmp libraries. Read ip address from .txt file, then extract snmp information and write in nuew .txt csv based files.

The information can be pushed in SQL databases in  a future
